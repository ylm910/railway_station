<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.railway.mapper.StationMapper">
    
    <resultMap id="BaseResultMap" type="com.railway.entity.Station">
        <id column="id" property="id"/>
        <result column="station_name" property="stationName"/>
        <result column="station_code" property="stationCode"/>
        <result column="station_type" property="stationType"/>
        <result column="platforms" property="platforms"/>
        <result column="remark" property="remark"/>
    </resultMap>
    
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO stations(id, station_name, station_code, station_type, platforms, remark) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.stationName}, #{item.stationCode}, #{item.stationType}, #{item.platforms}, #{item.remark})
        </foreach>
    </insert>
    
</mapper>
